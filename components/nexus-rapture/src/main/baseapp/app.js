Ext.require([
    // We may not use all of the extjs framework, but we might as well include all of it so it's available for plugins
    '*',

    //
    // Require placeholder class to trigger Sencha CMD scanning of application-level scss files
    //
    'baseapp.Application'
]);
