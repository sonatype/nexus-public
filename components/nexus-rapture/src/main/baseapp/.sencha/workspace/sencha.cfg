workspace.build.dir=${workspace.dir}/../../../target
workspace.packages.dir=${workspace.dir}/packages
workspace.theme.dir=${workspace.packages.dir}/${args.themeName}

ext.dir=${workspace.dir}/../../../target/ext
